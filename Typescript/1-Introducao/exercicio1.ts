// O desafio é criar uma função que aceita dois argumentos, somente do tipo numérico (number);
// E exibe a soma entre os dois;

function soma(a: number, b: number) {
  console.log(a + b)
}

soma(10, 10)
